
function init(self)
	factory.create("/factories#m_exp", vmath.vector3(math.random(1280),math.random(720),0))
end

function update(self, dt)
		
end

function on_message(self, message_id, message)
end

function on_input(self, action_id, action)

end
